AFRAME.registerComponent("map", {
    schema: {

    },


    init: function () {
        window.addEventListener("keydown", e => {
            if (e.key == "j") {
                
                
            }
        })

        window.addEventListener("keydown", e => {
            if (e.key == "l") {

        
            }
        })
        window.addEventListener("keydown", e => {
            if (e.key == "i") {
                
                
            }
        })
        window.addEventListener("keydown", e => {
            if (e.key == "k") {
                
            }
        })
    }

})